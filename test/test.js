/*global describe, it, assert, before, after*/
'use strict';

describe('blabla test', function () {
  before(function(done) {
    this.skip();
    done();
  });
  it('blabla case', function (done) {
    done();
  });
});
